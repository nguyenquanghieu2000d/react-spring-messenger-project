import React from "react";
import WebSocketMainContainer from "../container/websocket/websocket-main-container";

export const WebsocketContextMain = () => {
    return (
        // <WebSocketProvider>
        <WebSocketMainContainer/>
        // </WebSocketProvider>
    )
}